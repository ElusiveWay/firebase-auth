import { QuestionsPipePipe } from './questions-pipe.pipe';

describe('QuestionsPipePipe', () => {
  it('create an instance', () => {
    const pipe = new QuestionsPipePipe();
    expect(pipe).toBeTruthy();
  });
});
